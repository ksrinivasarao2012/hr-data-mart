# ---------------------------------------------------------------------------
# HR Data Mart — local (no-Docker) requirements for Windows + Python 3.11/3.12
#
# Install with:   uv pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- transformation layer ---------------------------------------------------
dbt-core==1.9.4
dbt-postgres==1.9.0

# --- data generation --------------------------------------------------------
Faker==33.1.0
psycopg2-binary==2.9.10

# --- LLM enrichment + guardrails --------------------------------------------
groq==0.13.1
pydantic==2.10.4
tenacity==8.5.0
python-dotenv==1.0.1

# --- evaluation -------------------------------------------------------------
scikit-learn==1.5.2
tabulate==0.9.0
pytest==8.3.4

# NOTE: pandas / numpy / sqlalchemy are intentionally NOT required.
# The seed and enrichment scripts use psycopg2 directly, which keeps the
# dependency tree small and avoids version fights with dbt-core.
